import React from 'react'

type Props = {
    id: string
}

const StudentAccountAdminView = (props: Props) => {
    const {id} = props
  return (
    <div>StudentAccountAdminView</div>
  )
}

export default StudentAccountAdminView